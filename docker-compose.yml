services:
  app:
    build: .
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DISPLAY_URL=http://localhost:3001
    restart: unless-stopped

